import React from 'react';

const Header = () => {
    return (
        <header style={{
            width: '100%',
            height: '100px',
            background: 'red'
        }}></header>
    );
};

export const Main = () => {
    return <p>메인입니다.</p>;
};

export default Header;